/* @flow */

import React, { PureComponent } from 'react';

type Props = {||};

class Partners extends PureComponent<Props> {
  render (): React$Node {
    return <div>Partners</div>;
  }
}

export default Partners;
